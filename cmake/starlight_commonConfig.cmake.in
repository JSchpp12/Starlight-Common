@PACKAGE_INIT@

if(TARGET Starlight::starlight_common)
    set(starlight_common_FOUND TRUE)
    return()
endif()

include("${CMAKE_CURRENT_LIST_DIR}/starlight_commonTargets.cmake")

set(starlight_common_FOUND TRUE)
set(starlight_common_VERSION "@PROJECT_VERSION@")
